# Test Data Fixtures

This directory should contain the following JSON fixture files:

- `sample_customers.json` - Customer test data
- `sample_products.json` - Product test data  
- `sample_orders.json` - Order test data

**These files are intentionally missing for the demo!**

Use GitHub Copilot to generate realistic test data that matches the schemas defined in the main README.
